# Story Library (Text-Only)

Each story lives in its own folder:

- `meta.yaml` — title/description/tags
- `characters.yaml` — character roster (no voice assignment)
- `story.md` — narrative text

This library is the **source material**. Audio “versions” (voices/SFX/music) are generated later.
